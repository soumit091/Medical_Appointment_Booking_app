import '../../../conts/consts.dart';

class XrayDetails extends StatelessWidget {
  const XrayDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
